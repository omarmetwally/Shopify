query GetProductById($id: ID!) {
    product(id: $id) {
        id
        title
        description
        productType
        vendor
        totalInventory
        variants(first: 10) {
            edges {
                node {
                    id
                    priceV2 {
                        amount
                        currencyCode
                    }
                    selectedOptions {
                        name
                        value
                    }
                }
            }
        }
        images(first: 10) {
            edges {
                node {
                    originalSrc
                }
            }
        }
        onlineStoreUrl
    }
}
